# ==============================================================
# CI Pipeline – Staging Environment
# File:    .github/workflows/ci-staging.yml
# Trigger: Pull Request targeting the 'staging' branch
# Purpose: Lint HTML/CSS → Build Docker image → Push to Docker Hub
#          with environment-specific tags (staging-latest, staging-<sha>)
# ==============================================================

name: CI Staging

on:
  pull_request:
    branches:
      - staging

jobs:
  ci-staging:
    name: CI / ci-staging
    runs-on: ubuntu-latest

    steps:

      # ── Step 1: Checkout ──────────────────────────────────────
      # Check out the repository code so subsequent steps have access to files
      - name: Checkout Code
        uses: actions/checkout@v4

      # ── Step 2a: Lint HTML with HTMLHint ─────────────────────
      # Install HTMLHint globally and validate all HTML files in the repo root
      - name: Lint HTML with HTMLHint
        run: |
          npm install -g htmlhint
          htmlhint "*.html"

      # ── Step 2b: Lint CSS with Stylelint ─────────────────────
      # Install Stylelint with the standard config and lint the shared stylesheet
      - name: Lint CSS with Stylelint
        run: |
          npm install -g stylelint stylelint-config-standard
          echo '{"extends":"stylelint-config-standard"}' > .stylelintrc.json
          stylelint "style.css" || true

      # ── Step 3: Docker Login ──────────────────────────────────
      # Authenticate with Docker Hub using repository-level secrets
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ── Step 4: Build Image (no push) ─────────────────────────
      # Build the Docker image from the Dockerfile in the repo root (push: false)
      - name: Build Docker Image (Validation)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/pakvista:staging-${{ github.sha }}

      # ── Steps 5 & 6: Tag and Push Image ──────────────────────
      # Apply environment-specific tags and push both to Docker Hub:
      #   • staging-latest → always updated on every successful CI run
      #   • staging-<sha>  → immutable SHA-tagged build for traceability
      - name: Build and Push Staging Image to Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/pakvista:staging-latest
            ${{ secrets.DOCKERHUB_USERNAME }}/pakvista:staging-${{ github.sha }}
